# PVS Decore

## Overview
A React + Vite website for PVS Decore, a flooring company based in Curitiba, Brazil that specializes in vinyl and laminate flooring installation.

## Tech Stack
- React 18
- Vite 5
- TypeScript
- Tailwind CSS (via CDN)
- Framer Motion (animations)
- Lucide React (icons)
- React Router DOM (routing)

## Project Structure
- `index.html` - Main HTML entry point
- `index.tsx` - React app entry point
- `App.tsx` - Main app component with routing
- `components/` - Reusable UI components
- `pages/` - Page components
- `constants.ts` - App constants and data
- `types.ts` - TypeScript type definitions
- `public/` - Static assets

## Development
The dev server runs on port 5000 with Vite.

### Commands
- `npm run dev` - Start development server
- `npm run build` - Build for production
- `npm run preview` - Preview production build

## Configuration
- `vite.config.ts` - Vite configuration (host: 0.0.0.0, port: 5000, allowedHosts: true)
- `tsconfig.json` - TypeScript configuration

## Recent Changes
- 2026-01-14: Initial setup for Replit environment, configured Vite for port 5000 with allowed hosts
